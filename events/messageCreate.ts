import { Message } from 'discord.js';
import { ClientWithCommands } from '../main.js';

export const data = { name: 'messageCreate' };

export async function execute(message: Message, client: ClientWithCommands) {
    return;
}
